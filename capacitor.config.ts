import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'shop.quetzaltech.drive',
  appName: 'QuetzalTech Drive',
  webDir: 'web/dist',
};
export default config;
